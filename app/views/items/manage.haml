.float_right
  = link_to "New item", new_item_path
  |
  = link_to "Public view", items_path

%table{:width => "100%"}
  %tr
    %th &nbsp;
    %th Title
    %th Caption
    %th Category
    %th(colspan="3")
    
  - @items.each do |item|
    %tr
      %td= image_tag item.image.url(:thumb)
      %td= item.title
      %td= item.caption
      %td= item.category
      %td
        = link_to "show", item
        |
        = link_to "edit", edit_item_path(item)
        |
        = link_to "destroy", item_path(item), :method => :delete, :confirm => "This action is irreversible. Are you sure you want to continue?"